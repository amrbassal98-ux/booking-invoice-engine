apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  labels:
    app: booking-engine
type: Opaque
stringData:
  DATABASE_USER: "postgres"
  DATABASE_PASSWORD: "changeme-production-db-password"
  DATABASE_URL: "postgresql://postgres:changeme-production-db-password@postgres-svc:5432/booking_engine"
  JWT_SECRET: "changeme-production-jwt-secret"
  STRIPE_SECRET_KEY: "sk_live_placeholder"
  STRIPE_WEBHOOK_SECRET: "whsec_placeholder"
